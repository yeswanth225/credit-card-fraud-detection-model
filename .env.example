# Copy to .env and replace with local values
APP_ENV=development
API_BASE_URL=http://localhost:8000
SECRET_KEY=replace_with_local_secret
JWT_SECRET=replace_with_local_secret
DATABASE_URL=sqlite:///./app.db
